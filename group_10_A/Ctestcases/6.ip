/*
Mixed Arithmetic and Logical Expressions
*/
int main() {
    int x = 5;
    int y = 10;
    int result1 = (x + y > 10) && (y - x < 10); 
    int result2 = (x * 2 == y) || (y / 2 > x); 
    int result3 = (x * x > y) && (y * x == 0); 
}